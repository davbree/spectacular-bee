---
label: Menu Configuration
hide_body: true
is_partial: false
fields:
  - type: field_group_list
    name: actions
    label: Action Buttons
    description: List of action buttons
    config:
      labelField: label
    fields:
      - type: include
        name: include_action
        label: Action
        template: action
